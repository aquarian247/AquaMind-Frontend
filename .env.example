# AquaMind Frontend Environment Configuration
# Copy this file to .env for local development

# ===== API Mode Configuration =====
# Set exactly ONE of these to 'true' (not both)
VITE_USE_MOCK_API=true        # Use lightweight in-memory mock API (default for quick development)
# VITE_USE_DJANGO_API=false   # Uncomment to use real Django backend instead of mocks

# ===== Backend URLs =====
# Only used when VITE_USE_DJANGO_API=true
DJANGO_API_URL=http://localhost:8000  # URL of your Django backend

# ===== Development Settings =====
# Uncomment to change the default development server port (default: 5000)
# PORT=5000

# ===== Notes =====
# - For local development without Django, keep VITE_USE_MOCK_API=true
# - For integrated testing with Django running, set VITE_USE_DJANGO_API=true
# - In production, always set VITE_USE_DJANGO_API=true
# - CI environments should set appropriate variables based on test needs
