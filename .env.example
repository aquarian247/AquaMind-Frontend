# ─────────────────────────────────────────────────────────────
# AquaMind Frontend – Example Environment Configuration
# Copy this file to `.env` (DO NOT commit the real .env file!)
# Fill in values appropriate for your local, test, or production
# environment.  All values are plain strings.
# ─────────────────────────────────────────────────────────────

# -------------------------------------------------------------
# Front-end → Back-end connection
# -------------------------------------------------------------
# Base URL of the running Django API server.
# During local development this is typically http://localhost:8000
# (must include the scheme and port).
VITE_DJANGO_API_URL=http://localhost:8000

# When set to “true” the front-end proxies API requests to the
# Django back-end defined above.  When “false” the mock Express
# API (under server/mock-api.ts) is used instead.
VITE_USE_DJANGO_API=true

# -------------------------------------------------------------
# Express / Vite dev server
# -------------------------------------------------------------
# TCP port the combined Express + Vite dev server should listen on.
# Defaults to 5001 if not provided.
PORT=5001
